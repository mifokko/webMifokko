import { SafeUrl } from "@angular/platform-browser"
import { Url } from "url"

export interface Redes {
    youtube: string,
    facebook: string,
    instagram: string,
    whatsapp: string,
    twitter: string
}